# NurProxy orchestrator configuration (read by the systemd unit).
# Edit, then: systemctl enable --now nurproxy

# HTTP port for the dashboard + API.
NP_PORT=8080

# Where the SQLite database and state live.
NP_DATA_DIR=/var/lib/nurproxy
